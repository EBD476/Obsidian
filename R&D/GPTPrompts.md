
Load data from html file GPT4
```
From the HTML file, extract the name of product and price. Put the data on a table and
export it to a CSV file.

Here's the element of one product:
<span class="a-size-medium a-color-base a-text-normal">Amazon Fire TV 43▒ Omni Series
4K UHD smart TV, hands-free with Alexa</span>

Here's the element of the price:
<span class="a-price-whole">349<span class="a-price-decimal'>.</span></span>

In case the price of the product is missing, leave that price as a null data.

```

upload another file for combine with last f
```
This is the second page of the previous website. Use the HTML file to extract the data following the
same steps | described before
```